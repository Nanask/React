import React from "react";

function MyButton() {
  const btnStyle = {
    backgroundColor: "blue",
    color: "while",
  };

  return <button style={btnStyle}>등록</button>;
}

export default MyButton;
